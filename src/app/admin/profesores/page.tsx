export default function AdminProfesoresPage() {
  return (
    <div>
      <h1 className="titular text-[var(--brand-secondary)]">
        Profesores (Landing)
      </h1>
      <p className="mt-2 text-[var(--brand-gray)]">
        Profesores mostrados en el carrusel público de la página principal.
      </p>
      <div className="mt-8 rounded-xl border border-dashed border-border bg-white p-12 text-center text-[var(--brand-gray)]">
        CRUD de profesores públicos (pendiente)
      </div>
    </div>
  );
}
